---
applications:
  - name: nexus-ui
    memory: 128M
    disk_quota: 256M
    instances: 1
    timeout: 180
    buildpacks:
      - nginx_buildpack
    env:
      # Set this to the API route after: cf apps  (no trailing slash)
      API_URL: https://nexus-api.cfapps.<region>.hana.ondemand.com
